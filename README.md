# amplipore
This is the nextflow-friendly branch of amplipore. Use this branch to build a docker image that can be used with (local) nextflow development. Automatically builds and bakes the database into the image for convenience.

```bash
make build
```

Use the docker image:tag in nextflow.
